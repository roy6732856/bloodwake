import {buildUpgrades} from '../src/builds.js';
const frame=document.getElementById('game'),g=()=>frame.contentWindow.__bloodwake;
document.querySelectorAll('[data-role]').forEach(b=>b.onclick=()=>{g().toMenu();g().lobby.select(b.dataset.role,false);});
document.getElementById('upgrade').onclick=()=>{const game=g();game.start();game.ranks={armament:3,orbit:2,magnet:1,crit:1};game.offerUpgrade();game.pendingChoices=['storm','regen','frost'].map(id=>buildUpgrades.find(u=>u.id===id));game.emit('upgrade',game.pendingChoices);};
document.getElementById('evolved').onclick=()=>{const game=g();game.start({director:'classic'});game.hp=game.stats.maxHp=10000;game.ranks={armament:5,orbit:5,storm:5,ember:5,magnet:1,wisdom:1,regen:1};game.stats.orbit=5;game.checkEvolution();game.auto=true;for(let i=0;i<25;i++)game.spawnEnemy('brute',Math.sin(i*2.399)*8,Math.cos(i*2.399)*8);};
document.getElementById('diagnostic').onclick=()=>{const a=g().lobby.model.userData.variants[g().lobby.role],data=[];a.traverse(o=>{if(o.isMesh)data.push({name:o.name,material:o.material.name,shader:o.material.onBeforeCompile.toString().slice(0,65),position:o.position.toArray()});});document.querySelector('#info').textContent=JSON.stringify(data,null,1);};
const previewMode=new URLSearchParams(location.search).get('show');if(previewMode){document.querySelector('nav').style.display='none';let tries=0;const ready=setInterval(()=>{if(g()){clearInterval(ready);if(previewMode==='upgrade')document.getElementById('upgrade').click();else if(previewMode==='evolved')document.getElementById('evolved').click();else document.querySelector(`[data-role="${previewMode}"]`)?.click();}else if(++tries>200)clearInterval(ready);},100);}
