{
  "name": "bloodwake",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "dev:cloud": "npm run build && wrangler d1 migrations apply DB --local && wrangler dev",
    "cloud:setup": "node scripts/cloud-setup.mjs",
    "deploy": "npm run build && node scripts/deploy.mjs",
    "test:cloud": "node tests/cloud-integration.mjs"
  },
  "description": "3D top-down roguelite shooter with Cloudflare Workers and D1 cloud saves.",
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
